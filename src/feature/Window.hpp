// Script access to the game window: focus, attention, and the clipboard.

#pragma once

namespace awow::feature::window
{
    /// Declares the window and clipboard script functions.
    void Initialize();
}
