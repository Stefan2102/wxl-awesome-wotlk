// GetInventoryItemTransmog: what a player's equipment looks like, as opposed to what it is.

#pragma once

namespace awow::feature::inventory
{
    /// Declares the transmogrification script function.
    void Initialize();
}
